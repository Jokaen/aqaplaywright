import { test, expect } from '../../fixtures/userGaragePage.fixture.js';

test('User should be logged in and see Garage page', async ({ userGaragePage }) => {
  await expect(userGaragePage.addCarBtn).toBeVisible();
});

test('User should be logged in and see Garage page 2', async ({ userGaragePage }) => {
  await expect(userGaragePage.addCarBtn).toBeVisible();
});

test('User should be logged in and see Garage page 3', async ({ userGaragePage }) => {
  await expect(userGaragePage.addCarBtn).toBeVisible();
});

test('User should be logged in and see Garage page 4', async ({ userGaragePage }) => {
  await expect(userGaragePage.addCarBtn).toBeVisible();
});

test('User should be logged in and see Garage page 5', async ({ userGaragePage }) => {
  await expect(userGaragePage.addCarBtn).toBeVisible();
});

test('User should be logged in and see Garage page 6', async ({ userGaragePage }) => {
  await expect(userGaragePage.addCarBtn).toBeVisible();
});

test('User should be logged in and see Garage page 7', async ({ userGaragePage }) => {
  await expect(userGaragePage.addCarBtn).toBeVisible();
});
